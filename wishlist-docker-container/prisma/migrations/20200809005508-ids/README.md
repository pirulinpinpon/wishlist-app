# Migration `20200809005508-ids`

This migration has been generated by James at 8/9/2020, 1:55:08 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Purchase" DROP CONSTRAINT "Purchase_productId_fkey"

ALTER TABLE "public"."Product" DROP CONSTRAINT "Product_pkey",
ALTER COLUMN "id" SET DATA TYPE text ,
ALTER COLUMN "id" DROP DEFAULT,
ALTER COLUMN "id" DROP DEFAULT,
ADD PRIMARY KEY ("id");;
DROP SEQUENCE "Product_id_seq";

ALTER TABLE "public"."Purchase" DROP CONSTRAINT "Purchase_pkey",
ALTER COLUMN "id" SET DATA TYPE text ,
ALTER COLUMN "id" DROP DEFAULT,
ALTER COLUMN "id" DROP DEFAULT,
ALTER COLUMN "productId" SET DATA TYPE text ,
ADD PRIMARY KEY ("id");;
DROP SEQUENCE "Purchase_id_seq";

ALTER TABLE "public"."Purchase" ADD FOREIGN KEY ("productId")REFERENCES "public"."Product"("id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200809003416-merchant..20200809005508-ids
--- datamodel.dml
+++ datamodel.dml
@@ -1,15 +1,15 @@
 datasource db {
   provider = "postgres"
-  url = "***"
+  url = "***"
 }
 generator prisma_client_js {
   provider = "prisma-client-js"
 }
 model Product {
-  id        Int        @default(autoincrement()) @id
+  id        String     @id @default(uuid())
   createdAt DateTime   @default(now())
   updatedAt DateTime   @updatedAt
   title     String?
   image     String?
@@ -18,11 +18,11 @@
   purchases Purchase[]
 }
 model Purchase {
-  id        Int      @default(autoincrement()) @id
+  id        String   @id @default(uuid())
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
   price     Float
-  productId Int?
+  productId String?
   product   Product? @relation(fields: [productId], references: [id])
 }
```


